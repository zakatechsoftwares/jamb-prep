# tools

Internal, non-shipping scripts.

- `docx-to-md.py` — one-off import tool used to convert the implementation
  plan from `.docx` to Markdown. Not part of any ongoing workflow.

The item generation pipeline (generation, independent-solve check, human
review queue) will live here as it's built.
