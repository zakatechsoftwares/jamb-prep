# Server-side only — used by next.config.mjs's rewrite proxy, never sent to
# the browser. The reviewer workspace's client code calls same-origin
# /api/review/... and Next.js forwards those requests here.
API_BASE_URL=http://localhost:3000
